import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App'; // Start our app away from root render. Keeps the app tidy.

ReactDOM.render(<App />, document.getElementById('root'));